node_modules/
dist/
.astro/
.env
.env.*
!.env.example
*.log
.DS_Store
google-service-account.json
_credentials/
_pipeline/
_audits/
output/
.playwright-mcp/
_sourcing/
_test-batch/
.wrangler/

# Generated client site trees (full Astro copies) — never commit
clients/

# Per-prospect report artifacts staged for the groundwork-reports Pages
# project (direct-upload deploy replaces the full asset set, so this dir
# must persist locally — like clients/). Functions + README are committed.
reports-site/public/audits/
reports-site/public/pitch/

# Local reference imagery for manual comparison — large, machine-local
_references/

# Local pipeline memory / caches
_memory/runs.jsonl
_memory/images/
_memory/backups/
_memory/practice-dashboard.html

# Design-catalog capture noise
docs/design-catalog/_paste-prompt.txt
docs/design-catalog/runs/*/audit.json
docs/design-catalog/runs/*/iter-*.json
docs/design-catalog/runs/*/capture/

# Grade / smoke temp outputs
/tmp-grade*.json
